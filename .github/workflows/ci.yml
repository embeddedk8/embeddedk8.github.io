name: CI

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  health-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: CI placeholder
        run: |
          echo "CI pipeline ready — checks will be added later"
